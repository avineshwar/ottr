---
name: Actions CI
on:
  pull_request: {}
  push:
    branches:
      - main
      - development
    tags:
      - 'v*.*.*'

jobs:
  testing:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python:
          - 3.9
        task:
          - name: Ottr ACME Package Unit Test
            command: |
              tox -e coverage-acme
          - name: Ottr API Unit Test
            command: |
              tox -e coverage-api
    name: "Python ${{ matrix.python }}/${{ matrix.task.name }}"
    steps:
      - uses: "actions/checkout@v2"
      - uses: "actions/setup-python@v2"
        with:
          python-version: ${{ matrix.python }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tox tox-gh-actions ottr-airbnb
      - name: ${{ matrix.task.name }}
        run: ${{ matrix.task.command }}